#include "pch.h"
#include "Client.h"
#include <iostream>
#include <string>

using namespace std;

Client::Client(string name, string address, long phone, string email, string birthDate) {

	name = name;
	address = address;
	phone = phone;
	email = email;
	birthDate = birthDate;

}